"use strict";

var getPhysicsFunction = function (primaryEntity) {
    return function () {
	// move primary entity:
	// if keydown left
	//    set velocityX left
	// else if keydown right
	//    set velocityX right
	// else
	//    set velocityX 0
	
	// if keydown up
	//    set velocityY up
	
	// apply acceleration
	// apply velocity

	// if hit ground
	//    set velocityY 0
	//    set positionY on ground
    }
}
